"""Utility scripts shipped with BEANS-Next.

These modules are intentionally small, CLI-oriented helpers. They must remain
CPU-only and use the same public runner primitives as `beans-next run` so that
artifacts are comparable.
"""
