# BEANS-Next NatureLM-audio 1.1 launcher (isolated venv).
#
# Real-mode dependency policy:
# - The v1.1 launcher is launcher-only and may depend on heavy ML deps.
# - Keep BEANS-Next core (`pyproject.toml`) free of these.
#
# This requirements set is intentionally aligned with the authoritative ESP research
# environment used for NatureLM-audio inference:
#   esp-research @ david-multi-audio branch, projects/NatureLM-audio-v1.5/pyproject.toml
#
# Notes:
# - Torch wheels (CPU/CUDA) may need a custom index URL on Slurm; the serve script can install
#   torch from a site-appropriate index if needed.

fastapi==0.115.12
uvicorn[standard]==0.34.2

pydantic==2.11.7
pydantic-settings>=2.12.0
typing-extensions>=4.15.0

requests==2.32.3
huggingface-hub>=1.3.0

jinja2>=3.1.6
numpy>=1.26.4
soundfile>=0.12.0
librosa>=0.9.2
resampy>=0.4.0
pyyaml>=6.0.0
cloudpathlib[gs]>=0.20.0

# Model runtime
transformers>=5.3.0
torch>=2.5.0
sympy>=1.13.1
peft>=0.18.1

# Optional (may be needed depending on checkpoint / quantization choices)
bitsandbytes>=0.43.0
torchao>=0.8.0

