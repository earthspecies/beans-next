# af3 launcher deps.
# Serve deps (required for both stub and real inference):
fastapi==0.115.12
uvicorn[standard]==0.34.2
pydantic==2.11.7

# Real inference deps (GPU required; install when AF3_STUB is not set).
# PyTorch: use the PyTorch cu124 index so the wheel matches the cluster driver
# (see pyproject.toml [tool.uv]; prefer `uv sync --group gpu` in this directory).
torch==2.5.1
# AF-Next (`nvidia/audio-flamingo-next-hf`) needed an unreleased fork when this
# launcher was written. Released Transformers now ships the model
# (`AutoProcessor` resolves to `MusicFlamingoProcessor`), so we use the canonical
# release rather than a personal fork -- results from a fork are hard to defend,
# and the fork also inlines the multi-audio batch-parity check inside
# `__call__`, where it cannot be corrected without reimplementing the whole
# processing routine. Upstream keeps that check in `validate_inputs`, which the
# launcher replaces with the correct placeholder/audio parity test (see
# `_install_multi_audio_validation` in serve.py).
transformers==5.16.1
huggingface-hub>=1.5.0,<2.0
accelerate>=1.1.0
soundfile>=0.12.1
librosa>=0.11.0

