# af3 launcher deps.
# Serve deps (required for both stub and real inference):
fastapi==0.115.12
uvicorn[standard]==0.34.2
pydantic==2.11.7

# Real inference deps (GPU required; install when AF3_STUB is not set).
# PyTorch: use the PyTorch cu124 index so the wheel matches the cluster driver
# (see pyproject.toml [tool.uv]; prefer `uv sync --group gpu` in this directory).
torch==2.5.1
# NOTE: AF-Next (`nvidia/audio-flamingo-next-hf`) may require unreleased Transformers support.
# The upstream model card documents a known-good fork branch for AudioFlamingoNext.
transformers @ git+https://github.com/lashahub/transformers.git@205f8c852db63e03c4d05a0e28ceee25f80806da
huggingface-hub>=1.5.0,<2.0
accelerate>=0.27.0
soundfile>=0.12.1
librosa>=0.11.0

